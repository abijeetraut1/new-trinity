<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />

style.

    *{
        font-family: Montserrat, sans-serif;
    }
    .admin-pannel-navbar{
        display: flex;
        justify-content: right;
        align-items: center;
        height: 4rem;
        padding: 0 2rem;
    }

    .admin-pannel-navbar div{
        margin-left: 1rem;
        color: white;
    }

    .mobile-nav {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        background-color: #333;
        z-index: 999;
    }

    .menu-btn {
        display: block;
        padding: 10px;
        cursor: pointer;
        width: fit-content;
    }

    .menu-icon {
        position: relative;
        z-index: 50;
        width: fit-content;
    }

    .nav-links {
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        //- background-color: #222;
        transition: left 0.3s ease;
    }

    @media (min-width: 732px){
        .nav-links {
            position: absolute;
            top: 0;
            left: -100%;
            width: 20%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            //- background-color: #222;
            transition: left 0.3s ease;
        }
    }

    .menu-icon i{
        width: fit-content;
    }
    .nav-links.show {
        left: 0;
    }

    .nav-links div a i{
        margin-right: 20px;
        overflow: hidden;
    }

    .nav-links div a , 
    .nav-links div h5{
        display: block;
        padding: 15px 20px;
        color: #ff3f6c;
        text-decoration: none;
        width: fit-content;
        cursor: pointer;
    }



//- for mobile device
<div class="mobile-nav">
    <div class="menu-btn">
        <div class="menu-icon">
            <i class="fa-solid fa-bars"></i>
        </div>
    </div>
    div.nav-links
        div
            h5 User Page
        div
            a(href="/")
                <i class="fa-solid fa-house"></i>
                span Home
            a(href="/")
                <i class="fa-solid fa-pen-nib"></i>
                span Design
            div
              h5 Admin Pannel
            div
              a(href="/admin/dashboard_products")
                <i class="fa-solid fa-phone"></i>
                span Order Received
            div
              a(href="/admin/dashboard-dispatched") 
                <i class="fa-solid fa-truck"></i>
                span Order Dispatched
            div
              a(href="/admin/dashboard-material-type") 
                <i class="fa-solid fa-shirt"></i>
                span Cloth Type

            div
              a(href="/admin/dashboard-fabric") 
                <i class="fa-solid fa-table-cells"></i>
                span Cloth Fabric
            div
              a(href="/admin/dashboard-show-users")
                <i class="fa-solid fa-user"></i>
                span Appoint Admin
            div
              a(href="/admin/dashboard-database-clear") 
                <i class="fa-solid fa-trash"></i>
                span Clear Database
</div>


script(src="../js/jquery-latest.js")
script.

    const menuBtn = document.querySelector(".menu-btn");
    const navLinks = document.querySelector(".nav-links");

    if(window.outerWidth > 1200){
        navLinks.classList.add("show");     
        menuBtn.style.display = "none";
    }else{
        document.addEventListener("DOMContentLoaded", function() {
            menuBtn.addEventListener("click", (el) => {    
                navLinks.classList.toggle("show");
            });
        });
    }

